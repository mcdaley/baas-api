# -----------------------------------------------------------------------------
# openapi/components/schemas/contact/contact-list.schema.yaml
# -----------------------------------------------------------------------------
type: object
properties:
  contacts:
    type: array
    items:
      $ref: './contact.schema.yaml'