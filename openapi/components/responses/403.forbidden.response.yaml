# -----------------------------------------------------------------------------
# openapi/components/responses/403.forbidden.response.yaml
# -----------------------------------------------------------------------------
description: >
  User does not have access to the resource
content:
  application/json:
    schema:
      $ref:     '../schemas/error/error.schema.yaml'
    example:
      code:     403
      message:  Access to resource is forbidden