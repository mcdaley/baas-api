# -----------------------------------------------------------------------------
# openapi/components/responses/401.unauthorized.response.yaml
# -----------------------------------------------------------------------------
description: >
  User was unauthenticated
content:
  application/json:
    schema:
      $ref:     '../schemas/error/error.schema.yaml'
    example:
      code:     401
      message:  User was not authenticated