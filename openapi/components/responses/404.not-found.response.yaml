# -----------------------------------------------------------------------------
# openapi/components/responses/404.not-found.response.yaml
# -----------------------------------------------------------------------------
description: >
  The specified resource was not found
content:
  application/json:
    schema:
      $ref:     '../schemas/error/error.schema.yaml'
    example:
      code:     404
      message:   Resource Not Found